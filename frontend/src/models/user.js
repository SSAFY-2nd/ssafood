export default class User {
    constructor (email, username, password, age, sex) {
        this.username = username;
        this.email = email;
        this.password = password;
        this.age = age;
        this.sex = sex;
    }
}